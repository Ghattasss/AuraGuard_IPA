// استيراد المكتبات المطلوبة
import 'package:flutter/material.dart';
import 'app.dart';

// الدالة الرئيسية للتطبيق - نقطة البداية
// هذه الدالة يتم استدعاؤها عند تشغيل التطبيق
void main() {
  // تشغيل التطبيق باستخدام widget الرئيسي App
  // runApp هي دالة Flutter التي تبدأ تشغيل التطبيق
  runApp(const App());
}
